# .evidence/gates.txt
#
# Format: <name>: <command>
# Lines starting with # and blank lines are ignored.
#
# Each command runs sequentially. Exit 0 = pass, anything else = fail. The
# stdout and stderr of each gate is captured to .evidence/<ts>-<name>.log.

typecheck: pnpm typecheck
lint: pnpm lint
test: pnpm test
